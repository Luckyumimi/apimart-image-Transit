version: "3.9"

services:
  apimart-image-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apimart-image-bridge
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
